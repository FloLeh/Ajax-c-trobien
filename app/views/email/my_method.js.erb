$(document).ready(function() {

  let new_email = $("<%= j (render 'layouts/email') %>").hide();
  $('.bodies').empty();
  $('.bodies').append(new_email);
  new_email.fadeIn();

  $('#<%= params[:id] %>').css({"font-style": 'italic', "font-weight":"normal"});
})
